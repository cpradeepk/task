<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) traffic for development -->
    <!-- This is required for connecting to local development server -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Local development IPs -->
        <domain includeSubdomains="true">192.168.0.13</domain>
        <domain includeSubdomains="true">192.168.1.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    
    <!-- Production domain uses HTTPS only -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">task.amtariksha.com</domain>
    </domain-config>
    
    <!-- Default: Allow HTTPS only for all other domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

